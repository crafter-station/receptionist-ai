{
  "name": "receptionist-ai",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "check": "tsc --noEmit",
    "dev": "bun run --hot hono.ts"
  },
  "dependencies": {
    "@kapso/whatsapp-cloud-api": "^0.3.0",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.3"
  }
}
